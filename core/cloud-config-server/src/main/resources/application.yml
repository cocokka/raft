spring.application.name: cloud-config-demo
server.port: 9999
spring.cloud.config:
  label: master
  server:
    git:
      uri: https://github.com/cocokka/config-repo
      searchPaths: /${spring.application.name}