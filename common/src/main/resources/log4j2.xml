<?xml version="1.0" encoding="UTF-8"?>
<configuration status="error">
    <Properties>
        <Property name="log_level">info</Property>
        <Property name="log_dir">/tmp</Property>
        <Property name="service_name">unknown-service</Property>
        <Property name="log_file">${sys:log_dir}/${service_name}.log</Property>
    </Properties>
    <!--先定义所有的appender-->
    <appenders>
        <!--这个输出控制台的配置-->
        <Console name="Console" target="SYSTEM_OUT">
            <!--控制台只输出level及以上级别的信息（onMatch），其他的直接拒绝（onMismatch）-->
            <ThresholdFilter level="trace" onMatch="ACCEPT" onMismatch="DENY"/>
            <!--这个都知道是输出日志的格式-->
            <PatternLayout pattern="%d{HH:mm:ss.SSS} %-5level %class{36} %L %M - %msg%xEx%n"/>
        </Console>
        <!--这个会打印出所有的信息，每次大小超过size，则这size大小的日志会自动存入按年份-月份建立的文件夹下面并进行压缩，作为存档-->
        <RollingFile name="RollingFile" fileName="${sys:log_file}"
                     filePattern="${sys:log_file}-%d{MM-dd-yyyy}.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS z} %-5level %class{36} %L %M - %msg%xEx%n"/>
            <!-- 日志文件大小 -->
            <SizeBasedTriggeringPolicy size="100MB"/>
            <!-- 最多保留文件数 -->
            <DefaultRolloverStrategy max="20"/>
        </RollingFile>
    </appenders>
    <!--然后定义logger，只有定义了logger并引入的appender，appender才会生效-->
    <loggers>
        <!--建立一个logger，此logger监听name对应的包名下的日志输出，level表示日志级别-->
        <Logger name="com.awu" level="trace" additivity="true">
            <AppenderRef ref="RollingFile"/>
        </Logger>
        <!--建立一个默认的root的logger-->
        <Root level="error">
            <!-- 在root中关联appender才会在对应的位置输出日志，定义一个输出到文件的appender也必须关联到此处才会保存日志到文件 -->
            <AppenderRef ref="Console"/>
        </Root>
    </loggers>
</configuration>